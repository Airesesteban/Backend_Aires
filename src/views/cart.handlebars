<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
</head>
<body>
    
<h1>Detalles del Carrito</h1>

<p>ID del Carrito: {{ cart._id}}</p>

<h2>Productos en el Carrito:</h2>
{{#if cart.products.length}}
<ul>
  {{#each cart.products}}
    <li>
      <p>Producto: {{ this.product.title }}</p>
      <p>Descripción: {{this.product.description}}</p>
      <p>Cantidad: {{ this.quantity }}</p>
      <button onclick="updateProductQuantity('{{../cart._id}}','{{this.product._id}}','{{this.quantity}}+1')">+</button>
      <button onclick="updateProductQuantity('{{../cart._id}}','{{this.product._id}}','{{this.quantity}}-1')">-</button>  
      <button onclick="deleteProduct('{{../cart._id}}','{{this.product._id}}')">Eliminar producto</button> 
    </li>
  {{/each}}
</ul>
{{else}}
  <p>No hay productos en el carrito.</p>
{{/if}}
<button onclick="deleteAllProducts('{{cart._id}}')">Vaciar carrito</button> 
<button onclick="purchase('{{cart._id}}')">Realizar compra</button>

<script src="/js/cart.js"></script>
</body>
</html>