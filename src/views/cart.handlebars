<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
</head>
<body>
    
<h1>Detalles del Carrito</h1>

<p>ID del Carrito: {{ _id }}</p>

<h2>Productos en el Carrito:</h2>
<ul>
  {{#each products}}
    <li>
      <p>Producto: {{ this.title }}</p>
      <p>Descripción: {{this.description}}</p> 
      <p>Cantidad: {{ quantity }}</p>
      <button onclick="updateProductQuantity('{{../_id}}','{{id}}','{{quantity}}+1')">+</button>
      <button onclick="updateProductQuantity('{{../_id}}','{{id}}','{{quantity}}-1')">-</button>  
      <button onclick="deleteProduct('{{../_id}}','{{id}}')">Eliminar producto</button> 
    </li>
  {{/each}}
</ul>
      <button onclick="deleteAllProducts('{{_id}}')">Vaciar carrito</button> 
      <button onclick="purchase('{{_id}}')">Realizar compra</button>  
<script src="/js/cart.js"></script>
</body>
</html>