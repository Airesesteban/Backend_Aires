<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de usuarios</title>
</head>
<body>

    {{#each users}}
  <div>
    <p>Nombre: {{fullName}}</p>
    <p>Email: {{email}}</p>
    <p>Rol: {{roles}}</p>
    
    <button onclick="changeRol('{{uid}}')">Modificar el rol</button>
    <button onclick="deleteUser('{{uid}}')">Eliminar usuario</button>
  </div>
{{/each}}
    <script src="/js/users.js"></script>
</body>
</html>